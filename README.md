# CineMatch
CineMatch is a Java application that reads movie and user data from external files, validates all fields, and generates personalized movie recommendations based on genres the user likes. The system enforces strict input validation rules for movie titles, movie IDs, user names, and user IDs.
